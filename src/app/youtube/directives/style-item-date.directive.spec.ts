import { StyleItemDateDirective } from './style-item-date.directive';

describe('StyleItemDateDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleItemDateDirective();
    expect(directive).toBeTruthy();
  });
});
