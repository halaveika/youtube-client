import { CountSortPipe } from './count-sort.pipe';

describe('CountSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CountSortPipe();
    expect(pipe).toBeTruthy();
  });
});
